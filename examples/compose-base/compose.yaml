name: dembly-compose-example

x-dembly:
  schema_version: 1

services:
  dev:
    image: alpine:3.21
    command: ["sleep", "infinity"]
    working_dir: /workspace
    volumes:
      - .:/workspace:rw
  database:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: example
